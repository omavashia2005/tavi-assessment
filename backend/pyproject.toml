[project]
name = "tavi-backend"
version = "0.1.0"
description = "Tavi voice intake service"
requires-python = ">=3.11"
dependencies = [
  "firecrawl-py>=4.30.2",
  "pipecat-ai[openai,runner,silero,webrtc]>=1.4.0",
  "pydantic>=2.12.0",
  "python-dotenv>=1.2.0",
]
